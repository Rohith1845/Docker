ARG VERSION
FROM almalinux:${VERSION:-9}
ARG COURSE="DOCKER" \
    TRAINER="ROHITH"
ENV COURSE=${COURSE}
RUN echo "TRAINER": ${TRAINER} , "COURSE": ${COURSE} , "VERSION": ${VERSION}
CMD [ "sleep" , "100" ]